version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.10
    commands:
      - echo "Installing dependencies (if any)..."
      - pip install --upgrade awscli

  build:
    commands:
      - echo "Preparing Lambda function files..."
      # If you have dependencies, install them into the current directory
      # pip install -r requirements.txt -t .
      - echo "Build phase completed"

artifacts:
  files:
    - lambda_functions.py
